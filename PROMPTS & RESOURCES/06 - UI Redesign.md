I need you to redesign the user experience and the UI of this app — it is way too cluttered. Ignore the current design system entirely. This app is aimed at vibe coders and agentic coders — it should have a fun, not corporate feel to it, and information should be easily accessible. The current design is way too cluttered with flashing lights, blinking dots, frame numbers, scanlines, mono-uppercase labels and weird film-strip elements everywhere. It looks like a cinema control room. That might work as a visual exercise but it's not practical for an application like this.

Use your front-end design skill to come up with something completely different. Light mode should be canonical. Warm cream canvas, espresso ink, one distinctive accent color used sparingly (I'd suggest a soft coral around `oklch(0.7 0.17 35)`). Pair a characterful display serif like Fraunces with a clean geometric sans like Geist, and reserve JetBrains Mono for code-like values only — model IDs, API keys, credit counts, code blocks. Sentence case everywhere, no `UPPERCASE` UI labels, no `font-mono uppercase tracking-[0.22em]` anywhere. Use rounded corners (`rounded-[10px]` for buttons/inputs, `rounded-2xl` for cards, `rounded-full` for pills), generous whitespace, friendly microcopy ("What should we make?" not "COMPOSE PROMPT"). Banish: grain overlays, scanlines, bracket frames, sprocket holes, glow-lime utilities, blinking cursors, frame counters, mask wipes, marching stripe bars, `rounded-none`, all the cinematic chrome.

While you're at it, the model capabilities need a careful review:

- **Seed** — research whether any current OpenAI gpt-image or Google Gemini image model actually accepts a seed parameter. They don't. Remove the seed UI entirely, stop sending it from the server action, and stop displaying it in the library/lightbox. Keep the DB column nullable for backfill but don't write to it.
- **Reasoning / thinking levels** — research which models support a `thinkingLevel` parameter. Gemini 3 Pro Image (Nano Banana Pro) supports `low` / `high`, and Gemini 3.1 Flash Image (Nano Banana 2) supports `minimal` / `high`. Expose this in the UI as a single "Default / Deep" toggle that only renders when the selected model declares thinking support. Wire the deep value through `providerOptions.google.thinkingConfig.thinkingLevel` in the AI SDK call.
- **Aspect ratios** — make the available chips depend on the selected model. Use a common subset of seven ratios (1:1, 3:2, 2:3, 16:9, 9:16, 4:3, 3:4) and add 21:9 only for Gemini 3 Pro. Declare this on each `ImageModelDefinition` via an `aspectRatios` field.

Also research the actual USD pricing for each image model (gpt-image-1.5, gpt-image-2, Gemini 2.5 Flash Image, Gemini 3.1 Flash Image, Gemini 3 Pro Image) and check it against the credit costs we have in the `model_pricing` table and the per-credit value of our Starter/Plus/Pro packs. If deep thinking meaningfully ups the cost on a model, charge more for it — add a `thinking_high_credit_cost` column on `model_pricing`, generate and run a drizzle migration, update the seed script, and use the surcharge value in `getModelCreditCost` when the user selected deep. Bump gpt-image-2 a credit or two if margins are tight at high quality. Record what thinking level was used by adding a `thinking_level` text column on the `generation` table.

For the landing page, this is critical: the audience cares about hooking the app up to their agents, not about pretty galleries. Cut the examples gallery entirely. Restructure the landing as:

1. **Hero** — two columns. Left: bold display headline, friendly subtitle, two CTAs (one to get an API key, one anchor link to the demo). Right: a tabbed MCP config card with tabs for Claude Desktop, Cursor, and Claude Code, each showing the actual config snippet (`mcpServers` JSON for Claude Desktop and Cursor, a `claude mcp add --transport http …` command for Claude Code). Include a real copy-to-clipboard button. The whole point is a developer can land here and copy the config in five seconds.
2. **Live demo** (new section, `id="live-demo"`) — two columns, looping mock. Left: a dark Claude-Code-style terminal that walks through (1) idle cursor, (2) user prompt visible ("Generate a hero image for our landing page — vintage red coupe in a brutalist parking garage at golden hour. Drop it into the `<Hero/>` component."), (3) agent "thinking…", (4) tool-call card with `get-images.generate_image({…})` and a "Generating…" spinner, (5) tool returns ✓ + agent says "Got it. Updating `src/components/Hero.tsx`…" with a "Writing file…" spinner, (6) edit complete ✓ + agent "Done." with a blinking cursor. Right: a fake browser-chrome'd SaaS landing page ("MyApp / Build something great."). Timing matters — **the website must stay in its placeholder state during the tool call** and only switch to an "Updating component…" loader once the agent starts writing the file; the image fades in only once the edit completes. Use a local optimized PNG (resize to 1280px wide, palette-quantized) under `public/demo.png` for the result image — I'll provide the source image.
3. **How it works** — three calm cards, kept.
4. **Final CTA** — kept.

Get rid of the dashboard topbar entirely. Each dashboard page owns its own inline header. The sidebar holds nav (Generate / Library / API keys / Integrations), the credit balance card, and a small user dropdown at the bottom that includes a Profile link, theme toggle, and Sign out. No frame numbers, no `[ Label ]` brackets, no blinking dots, no "Live / SYS OK" badge.

The generation page is the most important dashboard page. Lay it out as: page title + subtitle at the top, then a two-column working area (stacks on smaller screens). Left column holds the prompt composer, the settings (model select grouped by provider, aspect-ratio chips, style chips, thinking chips conditionally rendered), and the Generate button with the live credit cost next to it. Right column holds the result stage, mocked to the selected aspect ratio when empty. Below both columns, a "Recent" strip of session thumbnails. The result-stage generating state should be a soft drifting aurora — three coral/amber/plum radial-gradient orbs on `mix-blend-screen` with `filter: blur(40-55px)`, drifting on different phases (11s / 13s / 17s), with a diagonal sheen sweeping across every ~3.6s and a small status dot + "Cooking your image…" caption. No marching stripes, no scanlines — calm aurora only.

Gut the Profile page down to a single identity card (avatar, name, email, verified pill, member-since, "Back to dashboard" and "Sign out" buttons). Delete every "Coming soon" dialog (edit profile, security settings, email preferences). Strip the cinematic film-plate panel out of the auth layout and replace with a centered `max-w-md` column on a single `hero-glow` background. Simplify library filters (search + aspect select + favorites toggle + sort), keep the masonry-ish grid but make it a real responsive grid, and rebuild the lightbox as a `md:grid-cols-[1fr_300px]` layout with the image on the left and a clean metadata dl on the right — no seed field.

When you finish each phase, write the new design system to `DESIGN.md` (replace the existing one entirely). Name it something fresh — I called mine "Open Studio" but feel free to pick your own. Document the palette, type scale, component patterns, animation rules, model capability map, and credit pricing table. The DESIGN.md should be the source of truth so subagents and future work can reference it.

Use subagents to parallelize the page-level redesigns once the foundation (DESIGN.md + globals.css + layout fonts + model capabilities + DB migration) is in place. Run lint, typecheck and a production build at the end. Test the UI in a browser if you can.
