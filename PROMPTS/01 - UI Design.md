Hey there, you're starting off with a boilerplate project. You are allowed to delete any components and pages that are no longer required by our actual application. What we're trying to build is an application. It's basically an AI image generation platform.
The landing page will have some copy and maybe examples of how the process works. The users will then be able to sign into their dashboard, and from the dashboard they'll be able to do a few things:

1. They'll have a view where they can provide a prompt to generate a new image.
2. They'll have access to a library where they can view all their past generations.
3. They'll have access to an API key section or page where they can view all their API keys, revoke keys, delete keys, and obviously create new keys.

Fundamentally, how this app will work is users will be able to log in and generate images, but this app will also expose an MCP server which users can add to their AI agents. In that MCP server, they have to specify the API key which will authenticate their account in this platform.

Now, I do not want you to implement the backend functionality yet. Please don't worry about adding the actual functionality to create API keys or generate images or even build the MCP server. All we want to do at this stage is design the user interface. You can use mock data and components, but the entire flow should be available from the landing page all the way to the dashboard pages.

I need you to use your front-end design skill to create something unique and professional.

Our app is called "Get Images".
