I need you to move this section to the very top. This application is very much aimed at people who want to use this tool with agents primarily. I do have to then mention that the UI is just there for testing or for users to go back and retrieve previously generated images. The primary function of this application is for this to be connected to an agent, either something like a coding tool like Claude Code or Codex, or AI solutions like ChatGPT or Claude Desktop. So this will be supported by any agentech tool that supports MCP servers or agent skills.
So therefore I think that this section should be at the top and very prominent: "Bring Get Images into any agent.".
Furthermore, for the MCP server, we will be using a remote server with API Key Authentication, so you can just adjust the example accordingly.

Also, this section does not make sense:
02 — HOW IT WORKS

You can just focus this around the fact that you can integrate Get Images with any agentic or AI tool that supports MCP servers or agent skills. The user will ask the coding agent, and you can just assume something like Claude Code. The user might be building a website. They then ask Claude Code to generate a Yero image or some kind of stock image. Get Images will be called as a tool. Get Images will then generate the image and pass that back to Claude Code. Claude Code can then integrate that image into the website or whatever it's working on.
